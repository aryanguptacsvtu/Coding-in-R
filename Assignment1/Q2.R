# QUESTION :- 
#Overwrite the object from (1.) using the same sequence with the order reversed. 


# Reverse the sequence and store it back in 'a'
a <- rev(a)

# Print the reversed sequence
print(a)

# Print the length of the reversed sequence
print(length(a))