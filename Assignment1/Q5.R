# QUESTION :-
# Confirm that the length of the vector created in (4.) is 20. 


# Recreate the vector from Q4
vec <- c(c(6:12), rep(5.3, times = 3), -3, seq(102, 100, length.out = 9))

# Print the length of the vector
print(length(vec))
