# QUESTION :-
# Extract the first and last elements of your vector from (6.), storing them as a new object. 


# Extract the first element of the vector and store it in 'first'
first <- vec[1]

# Extract the last element of the vector and store it in 'last'
last <- vec[length(vec)]

# Print the first element
print(first)

# Print the last element
print(last)